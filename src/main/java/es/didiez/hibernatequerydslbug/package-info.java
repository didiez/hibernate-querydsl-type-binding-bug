@TypeDefs({
    @TypeDef(name = "A_I", typeClass = ActiveInactiveType.class)
})
package es.didiez.hibernatequerydslbug;

import org.hibernate.annotations.TypeDef;
import org.hibernate.annotations.TypeDefs;

